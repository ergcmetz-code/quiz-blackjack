services:
  - type: web
    name: blackjack-quiz-table
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app 
    disk: 
      name: quiz-data
      mountPath: /data
      sizeGB: 1
